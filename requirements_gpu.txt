# Gravity Yonder Over - GPU Requirements for NVIDIA-enabled PCs
# Install these packages ON TOP of the base requirements.txt for full GPU acceleration

# Core Streamlit and Web Framework
streamlit>=1.28.0
plotly>=5.15.0
kaleido>=0.2.1

# Data Processing and Scientific Computing
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# Visualization and UI
colorcet>=3.0.1
seaborn>=0.12.0
matplotlib>=3.7.0

# Machine Learning and Physics (CPU versions for cloud compatibility)
torch>=2.0.0,<2.1.0
torchvision>=0.15.0,<0.16.0
scikit-learn>=1.3.0
sympy>=1.12

# Mathematical Libraries
numba>=0.57.0

# Database and Storage
sqlalchemy>=2.0.0

# Utilities
json5>=0.9.0
pyyaml>=6.0
python-dotenv>=1.0.0

# Image and Data Processing
pillow>=9.5.0
imageio>=2.31.0

# Educational Content Management
markdown>=3.4.0
bleach>=6.0.0

# Development Tools (lightweight)
black>=23.7.0
isort>=5.12.0

# ========================================
# GPU-ACCELERATED NVIDIA TOOLS
# ========================================
# Install these for full NVIDIA GPU acceleration
# Requires NVIDIA GPU with CUDA 11.8+ or 12.0+

# RAPIDS cuDF - GPU-accelerated DataFrames
cudf>=23.08.0
cupy>=12.0.0
rapids-cuml>=23.10.0

# NVIDIA Modulus - Physics-Informed Neural Networks
nvidia-modulus>=0.3.0  # Install from conda-forge: conda install -c conda-forge modulus

# NVIDIA cuQuantum - Quantum Computing Acceleration
cuquantum>=23.08.0  # Install from conda-forge: conda install -c conda-forge cuquantum

# NVIDIA NeMo Toolkit - Neural Models
nemo-toolkit>=1.20.0  # Install via: pip install nemo_toolkit[all]

# NVIDIA Morpheus - AI Security and Analytics
morpheus>=23.08.0  # Install from conda-forge: conda install -c conda-forge morpheus

# Additional Performance Libraries
mypy==1.7.0
pre-commit==3.5.0

# Performance
cython==3.0.5

# Visualization
bokeh==3.3.1
altair==5.1.2

# Configuration
python-dotenv==1.0.0
pyyaml==6.0.1

# Logging
loguru==0.7.2

# Async
aioredis==2.0.1
celery==5.3.4

# Documentation
sphinx==7.2.6
sphinx-rtd-theme==1.3.0

# Monitoring
prometheus-client==0.19.0
sentry-sdk==1.38.0

# ========================================
# INSTALLATION NOTES FOR GPU USERS:
# ========================================
# 1. Install Miniconda/Anaconda first
# 2. Create a new environment: conda create -n gravity-yonder python=3.10
# 3. Activate environment: conda activate gravity-yonder
# 4. Install CUDA toolkit: conda install -c nvidia cuda-toolkit
# 5. Install RAPIDS: conda install -c rapidsai -c conda-forge -c nvidia rapids
# 6. Install Modulus: conda install -c conda-forge modulus
# 7. Install cuQuantum: conda install -c conda-forge cuquantum
# 8. Install remaining packages: pip install -r requirements_gpu.txt
